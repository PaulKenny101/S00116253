@model IEnumerable<RAD302CA.Models.Guest>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GuestnName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GuestnName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GuestId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GuestId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GuestId })
        </td>
    </tr>
}

</table>
